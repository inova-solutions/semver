export const MIN_GIT_VERSION = '2.22.0';
export const CLI_NAME = 'semver-cli';
export const CONFIG_FILE = '.semver.json';

export const ERRORS = {
  UNKNOWN_BRANCH: `branch not recognized, use ${CONFIG_FILE} for configuration`
};
